<h1 class="section-title">Películas</h1>
<div class="row">    
    {{#each movies}}
    <div class="col-md-3 mb-2 mt-2">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title d-flex justify-content-between align-items-center">
                    {{title}} <a href="/movies/edit/{{_id}}"><i class="fa fa-pencil"></i></a>
                </h4>
                <p>{{director}}</p>
                <p>{{year}}</p>
                <form action="/movies/delete/{{_id}}?_method=DELETE" method="POST">
                    <input type="hidden" name="_method" value="DELETE">
                    <button type="submit" class="btn btn-danger btn-block btn-sm">Eliminar</button>
                </form>
            </div>
        </div>
    </div>
    {{/each}}
</div>
<div class="jumbotron m-0 p-4">
    <p class="lead"><span><b>NOTA</b>:</span> Éste módulo se alimenta desde la apiRest de películas, es por eso que puede llevar un 
        poco más de tiempo en procesar las peticiones y mostrar los resultados.
    </p>
</div>